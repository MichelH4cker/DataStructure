#include "insertion.h"

void insertion(){
    List* li = createList();
    Node node;
    li node;

    for (int j = 0 ; input != 0; j++){ //run datas of each list
        scanf("%f", &input);
        
    }
    arrayLists[positionArray] = ;
}